b=int(input())
a=input()
q=list(a.split())
w=(min(q))
k=0
e=""
for i in range(0,len(min(q))-1):
  for j in range(0,len(min(q))):
    if(q[i][j]==q[i+1][j]):
      e+=(q[i][j])
    else:
      print(-1)
      break
  break
print(e)




Output Format

prefix - common prefix in all the given strings.

Sample Input 0

4
best bestfriend bestman bestgirl
Sample Output 0

best
Explanation 0

The string best is the common prefix present in all the given strings.
Sample Input 1

3
beautiful wonderful hateful
Sample Output 1

-1
Explanation 1

The given strings have no common prefix
